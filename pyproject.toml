[project]
name = "arxiv-search"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Rafid Al-Humaimidi", email = "rafidka@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
arxiv-search = "arxiv_search:main"

[build-system]
requires = ["uv_build>=0.8.13,<0.9.0"]
build-backend = "uv_build"
